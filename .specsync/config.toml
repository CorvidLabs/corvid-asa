# SpecSync Configuration
[specsync]
# Path to the specs directory
specs_dir = "specs"

# Default module template to use when creating new specs
default_template = "default"

# Whether to enforce spec validation
enforce_validation = true